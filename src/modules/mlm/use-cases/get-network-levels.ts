/**
 * Get Network Levels Use Case
 *
 * Retrieves the complete MLM network structure for a user (N1, N2, N3).
 * This is the main use case for fetching the genealogy tree.
 */

import { getNetworkLevels } from "@/modules/mlm/helpers/network";

export { getNetworkLevels };
