DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mvppir?schema=public"
REDIS_URL="redis://localhost:6379"
PORT=3333
NODE_ENV=development

# Authentication
AUTH_SECRET="your-secret-key-here-at-least-32-chars-long-please-change-this"
API_BASE_URL="http://localhost:3333"
FRONTEND_URL="http://localhost:3000"

# Moralis (Blockchain Integration)
MORALIS_API_KEY="your-moralis-api-key-here"
# Stream Secret: Defina um secret qualquer aqui e use o MESMO ao criar o Stream no painel Moralis
MORALIS_STREAM_SECRET="create-your-own-secret-and-use-it-when-creating-stream"

# Encryption (AES-256-GCM for private keys)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="your-64-char-hex-encryption-key-here-32-bytes-in-hex"

# Polygon Network
POLYGON_RPC_URL="https://polygon-rpc.com"
POLYGON_CHAIN_ID="137"

# Global Wallet (v2.0)
# A partir do v2.0, a Global Wallet é armazenada no banco de dados (tabela global_wallets)
# Use o script: npx tsx scripts/create-global-wallet.ts
# A private key fica ENCRIPTADA no banco, não no .env
# Apenas a ENCRYPTION_KEY (acima) é necessária para descriptografar
